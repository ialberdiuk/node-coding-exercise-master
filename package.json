{
  "name": "coding-exercise-master",
  "version": "1.0.0",
  "description": "knack-coding-challenge",
  "main": "src/index.js",
  "author": "Ivan Alberdi",
  "license": "ISC",
  "scripts": {
    "start": "node ./src/index.js",
    "lint": "eslint 'src/*.js'",
    "fix": "eslint --fix 'src/**/*.js'",
    "watch": "nodemon",
    "build": "babel src -d dist",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watchAll",
    "test:coverage-all": "jest --coverage --watchAll"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "eslint": "^8.33.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "jest": "^29.4.1"
  }
}
